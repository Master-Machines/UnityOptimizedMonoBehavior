using UnityEngine;
using System.Collections;

public class UnoptimizedBehavior : MonoBehaviour {

	void Update () {
        /** Blank, but Unity will strip out the Update() call if it is not defined. 
        In a more advanced implementation of OptimizedBehavior, reflection could be used to do the same. */
	}
}
